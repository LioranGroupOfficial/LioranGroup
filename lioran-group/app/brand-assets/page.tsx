export default function BrandAssetsPage() {
  return (
    <div className="page-shell page-grid">
      <section className="page-intro">
        <span className="eyebrow">Brand Assets</span>
        <h1>Brand assets and usage rules</h1>
        <p>
          Public brand assets should follow the same strict palette and layout
          rules used throughout the site. Decorative variants are intentionally
          excluded.
        </p>
      </section>
    </div>
  );
}
